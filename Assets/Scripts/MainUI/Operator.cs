using UnityEngine;
using System.Collections;

public class Operator : MonoBehaviour {

	// Use this for initialization
	void Start () {
        this.GetComponent<Camera>().aspect = 1.3333f;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
